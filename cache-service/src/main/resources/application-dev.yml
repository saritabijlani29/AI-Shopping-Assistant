server:
  port: 8083

spring:
  data:
    redis:
      host: redis        # This matches your Docker service name
      port: 6379
      connect-timeout: 6000ms

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always